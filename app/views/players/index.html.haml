%table{class: "table table-striped table-condensed table-hover"}
  %thead
    %tr
      %th= t(:first_name)
      %th= t(:last_name)
      %th= t(:email)
      %th{colspan: 4}
  %tbody
    - @players.each do |player|
      %tr
        %td= player.first_name
        %td= player.last_name
        %td= player.email
        %td= link_to t(:characters), player_characters_path(player), class:"btn btn-info pull-right"