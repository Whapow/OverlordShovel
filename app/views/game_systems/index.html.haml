%table{class: "table table-striped table-condensed table-hover"}
  %thead
    %tr
      %th= t(:name)
      %th
      %th{colspan: 2}
  %tbody
    - @game_systems.each do |game_system|
      %tr
        %td= game_system.name
        %td
          = link_to t(:delete), game_system, method: :delete, data: {confirm: "Are you sure?"}, class:"btn btn-danger pull-right"
          = link_to t(:edit), edit_game_system_path(game_system), class:"btn btn-info pull-right"
    %tr
      %td= link_to t(:add_game_system), new_game_system_path, 'data-no-turbolink' => true
      %td
      %td